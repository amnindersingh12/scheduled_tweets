<h2>Displaying Tweets..</h2>
<%# <%= link_to 'Back', :back %> 

<table class="table table-hover">
  <thead class="thead-dark">
  <th></th>
  </thead>
  <tbody>
  
    <td> <%= @tweet.body %></td>
    <td><%= @date_time %></td>
      <td><%= button_to "Delete", tweet_path(@tweet.id), method: :delete ,  class: "mt-3 px-6 py-2 bg-sky-400 text-white font-semibold rounded-full text-center cursor-pointer inline-block hover:bg-sky-500 transition ease-in-out duration-300" %></td>
  </tbody>
</table>
